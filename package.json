{
  "name": "request-logger",
  "version": "1.0.0",
  "description": "Cloudflare Workers request logger with optional encryption",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240423.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.55.0"
  }
}
